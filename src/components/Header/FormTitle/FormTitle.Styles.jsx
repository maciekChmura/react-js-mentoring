import styled from 'styled-components';

const H2 = styled.h2`
  grid-area: 2 / 2 / 3 / 3;
  font-size: 18px;
  color: white;
  width: auto;
  height: auto;
`;

H2.displayName = 'H2';

export default H2;
